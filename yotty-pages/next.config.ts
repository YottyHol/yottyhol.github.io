import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/yottyhol.github.io', // Replace with your GitHub repo name
  assetPrefix: '/yottyhol.github.io/', // Also replace with your repo name
  // Other config options can go here
};

export default nextConfig;
